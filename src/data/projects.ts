// This file is intentionally empty. The Projects section is a project management
// system: it never seeds, hardcodes, or invents projects. Projects are persisted
// by the Projects component (localStorage) and are only added through the interface.

import { Project } from '../types';

export const projectsData: Project[] = [];